let nombreCompleto = prompt('dime el nombre')
    for(let i = 0; i < nombreCompleto.length; i++){
        console.log(nombreCompleto.charCodeAt(i).toString())
        let unicode = nombreCompleto.charCodeAt(i).toString()
    }